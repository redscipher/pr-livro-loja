{
    "version": 2,
    "builds": [
      {
        "src": "prlivroloja/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
      }
    ],
    "routes": [
      { "src": "/static/(.*)", "dest": "/static/$1" },
      { "src": "/(.*)", "dest": "prlivroloja/wsgi.py" }
    ]
  }
  